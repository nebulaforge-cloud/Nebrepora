{
  "name": "nebrepora",
  "version": "1.0.0",
  "private": true,
  "description": "Nebrepora — signal board for early open-source and dev-tool breakthroughs.",
  "type": "module",
  "license": "UNLICENSED",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/prerender.mjs",
    "check": "node --check public/app.js && node --check public/view.js && node --check public/data.js && node scripts/prerender.mjs --check",
    "assets": "node scripts/render-assets.mjs",
    "dev": "npx --yes serve@14 -l 5173 public"
  }
}
